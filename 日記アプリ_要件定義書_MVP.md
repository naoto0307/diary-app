# 日記アプリ 要件定義書（MVP版）

version: 0.1（初版）
最終更新：2026年8月

---

## 1. コンセプト

**「書いて、見返せる」ことだけに機能を絞った日記アプリ。**

多機能化する前に、まず「毎日書いて、後から読み返す」という日記の核となる体験を最短距離で提供する。機能は本要件書をベースラインとして、リリース後に利用状況を見ながら都度追加していく。

## 2. スコープ

### 2-1. MVPに含める機能（Must）

| No | 機能 | 概要 |
|---|---|---|
| F1 | 日記の新規作成 | テキストで日記を書いて保存する |
| F2 | 日記の一覧表示 | 過去に書いた日記を日付順に並べて見る |
| F3 | 日記の詳細表示 | 一覧から選んだ1件の日記を全文表示する |
| F4 | 日記の編集 | 書いた日記の内容をあとから修正する |
| F5 | 日記の削除 | 不要になった日記を消す |

これ以外の機能（検索、タグ、写真添付、気分記録、カレンダー表示、通知、クラウド同期、パスコードロックなど）は**すべてMVP対象外**とし、8章「将来拡張候補」に切り出す。

### 2-2. 明示的に対象外（Won't do / この段階では判断しない）

- アカウント登録・ログイン機能（データは端末内で完結するため不要）
- 他ユーザーとの共有・SNS的機能
- AIによる分析・要約・プロンプト機能
- 複数の日記帳（ジャーナル）の使い分け
- 広告・課金機能

## 3. 対象プラットフォーム／ユーザー

- **対応OS**：iOS、Android（両OSで同等の機能を提供する）
- **対応端末**：スマートフォン（タブレット最適化は対象外）
- **想定ユーザー**：日記を手軽に書き始めたい個人ユーザー。既存の多機能アプリに機能過多で挫折した層も想定。
- **通信環境**：オフラインでも全機能が使えること（後述4-2）

## 4. 非機能要件

### 4-1. プラットフォーム方針

iOS/Android両対応のため、**単一コードベースのクロスプラットフォーム開発**を推奨する。

| 選択肢 | 概要 | 推奨度 |
|---|---|---|
| Flutter | Dart言語。UIを自前描画するため両OSで見た目が揃えやすく、ローカルDBまわりのライブラリも成熟している。 | ◎ 推奨 |
| React Native | JavaScript/TypeScript。Web開発の知見を転用しやすい。 | ○ 代替案 |
| ネイティブ個別開発（Swift＋Kotlin） | 品質は最も高いが2倍の開発コストがかかり、「都度追加していく」MVP方針とは相性が悪い。 | △ 非推奨（MVP段階では） |

→ 本書ではFlutterを前提に以降の要件を記載するが、開発者の得意技術に応じてReact Nativeへの読み替えは可能。

### 4-2. データ保存方針（オフラインファースト）

- 日記データは**端末内のローカルDB（SQLite想定）に保存**し、ネットワーク接続なしで全機能が使えることを必須とする。
- 将来クラウド同期を追加しやすいよう、以下をMVPの段階からデータ設計に織り込んでおく（＝今は同期機能を作らないが、後で作れるように土台だけ用意する）。
  - 各日記データに**UUID**を主キーとして持たせる（連番IDだと後から複数端末をマージする時に衝突するため）
  - `created_at`（作成日時）に加え`updated_at`（更新日時）を持たせる（将来の同期時に「どちらが最新か」を判定できるようにするため）
  - 論理削除の余地を残す（`deleted_at`カラムなど）。ただしMVPでは物理削除でよく、カラムの追加のみ将来対応でも可。

### 4-3. セキュリティ・プライバシー

- 日記は個人の機微な内容を含みうるため、**MVPの直後に優先度高で検討すべき事項**として以下を記録しておく（本MVPスコープには含めない）。
  - 端末のパスコード／生体認証（Face ID・指紋）によるアプリロック
  - バックアップ時のデータ暗号化
- MVP時点でも、OS標準の暗号化（iOSのファイル保護、Androidのアプリサンドボックス）の範囲は最低限担保される想定。

### 4-4. パフォーマンス・可用性

- 日記1件あたりの文字数上限は設けない（実用上、数千〜数万文字は許容できる設計とする）
- 一覧表示は、件数が増えても操作がもたつかないこと（数百〜数千件を想定したスクロール性能）
- クラッシュ等でのデータ消失を避けるため、保存操作は都度ローカルDBへ確定保存する（メモリ上にのみ保持しない）

## 5. 画面構成

MVPでは以下3画面のみとする。

```
[日記一覧画面] ──タップ──▶ [日記詳細画面] ──編集ボタン──▶ [作成・編集画面]
       │                                                        ▲
       └─────────────────────新規作成ボタン────────────────────┘
```

### 5-1. 日記一覧画面（ホーム画面）

- 起動時に表示される最初の画面
- 過去に書いた日記を**日付の新しい順**にリスト表示
- リストの各行には以下を表示
  - 日付（例：2026年8月4日）
  - 本文の冒頭数十文字（プレビュー）
- 日記が1件もない場合は、空状態のメッセージと「最初の日記を書く」導線を表示
- 画面右下などに「新規作成」ボタンを常設

### 5-2. 日記詳細画面

- 一覧から選択した1件の日記全文を表示
- 表示項目：日付、本文全文
- 「編集」ボタンと「削除」ボタンを設置
- 削除時は誤操作防止のため確認ダイアログを表示

### 5-3. 作成・編集画面

- 本文を入力するテキストエリア（複数行、スクロール対応）
- 保存すると自動的に「今日の日付」を記録（日付の手動変更はMVP対象外）
- 「保存」と「キャンセル」の導線を用意
- 入力途中で画面を離れようとした場合、破棄確認を出す（誤って内容を消さないため）

## 6. データモデル（案）

```
Entry（日記エントリー）
------------------------------
id            : UUID       // 主キー
body          : TEXT       // 本文（必須）
created_at    : DATETIME   // 作成日時
updated_at    : DATETIME   // 更新日時（編集の都度更新）
```

- タイトルフィールドはMVPでは持たない（本文冒頭をプレビューとして流用するため）。将来必要になれば`title`カラムを追加する形で拡張可能。
- 1エントリー＝1回の保存単位とし、「1日1件まで」という制約は設けない（1日に複数回書いてもよい）。この点は将来「1日1ページ」運用に倒すか、複数投稿を許容するSNS風運用にするかの意思決定ポイントとして残る（8章参照）。

## 7. 受け入れ基準（MVP完了の定義）

以下がすべて満たされればMVPとしてリリース可能とする。

- [ ] iOS・Android両方の実機で、新規作成・一覧表示・詳細表示・編集・削除の一連の操作ができる
- [ ] アプリを完全に閉じて再起動しても、保存した日記が消えずに残っている
- [ ] 機内モード（オフライン状態）でも、すべての操作が問題なく行える
- [ ] 日記が0件の状態でクラッシュせず、適切な空状態の画面が表示される
- [ ] 削除操作には確認ダイアログがあり、誤操作で日記が消えない

## 8. 将来拡張候補（優先度は未確定・都度検討）

初回調査（既存アプリ16本の比較）から示唆された、追加候補を参考情報として残す。

| 分類 | 候補機能 | 参考にした既存アプリ |
|---|---|---|
| 振り返り強化 | カレンダー表示、検索機能、タグ付け | Day One, Diarium, 引き出し日記 |
| 継続支援 | 通知リマインダー、連続記録日数（ストリーク） | Day One, 毎日まめ |
| 入力の幅 | 写真添付、音声入力 | Day One, Diarium, 日記Pics |
| セキュリティ | パスコード／生体認証ロック | Day One, muute |
| データ可搬性 | クラウド同期、テキスト/PDFエクスポート | Diarium, LiveIt |
| 気分記録 | ワンタップの気分・活動ログ | Daylio, 毎日まめ |
| 見た目 | テーマカラー・フォント変更 | Caho日記帳, 引き出し日記 |

※ここに挙げたものはあくまで候補であり、次フェーズの仕様書で優先順位と詳細仕様を別途検討する。

---

### 未決定事項（次回までに認識合わせが必要な点）

1. **1日に複数件書けるようにするか、1日1ページ運用にするか**（6章参照）
2. パスコードロックをMVP直後の最優先追加機能とするかどうか
3. Flutter／React Nativeいずれの技術で進めるか（開発者のスキルセットに依存）
